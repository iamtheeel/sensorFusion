
-----------
serial
sudo dmesg | grep ttyUSB
the first one is our guy
screen /dev/ttyUSB0 115200

logon mdt shell
logon: mendel
pass: mendel

network tool: nmtui

to get ssh to work:
mdt pushkey ~/.ssh/id_rsa.pub

--------------
extra installs
--------------
The keys from goole are missing, so,prior to apt-updated:
but apt-get update still doesn't work... the google paths seems to be nonexistant


sudo apt-key adv --keyserver pgp.mit.edu --recv-keys C0BA5CE6DC6315A3
sudo apt install net-tools


-----------
dev env
-----------
sudo apt install python3.11
    --draws too much power and flatlines?

pip3 install ultralytics
    -- packages not avalable
        * mkl_rt
        * blis
        * openblas

        no sudo apt install libatlas-base-dev
        sudo apt install gfortran

    -- no numpy version availble new enough
